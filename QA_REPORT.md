# MRT 路線進出站時間模擬器 V2.1.0 - QA 報告

驗收日期：2026-08-20

環境：Windows x64、.NET SDK 10.0.400、Microsoft Windows Desktop Runtime 10.0.11

## Release 建置結果

```text
MrtRouteSimulator.Engine  成功
MrtRouteSimulator.Tests   成功
MrtRouteSimulator.App     成功
0 個警告
0 個錯誤
```

另以 `dotnet publish -c Release --self-contained false` 建立 framework-dependent Windows 成品，並在 Windows x64 環境啟動驗收。

版本控制驗證：Release 主視窗標題與頁首均顯示 `V2.1.0`；Windows `ProductVersion = V2.1.0`、`FileVersion = 2.1.0.0`，與 `Directory.Build.props` 及 Engine 組件一致。

## 自動化測試

結果：`53/53 通過，0 失敗`

### V1.0 相容性（24 項）

- 無限制、梯形、三角形、零／負距離與非法列車性能。
- 車站里程累加、停站、全程逐項加總、起終點時間定義與折返循環。
- 理論／指定班距、列車 ID、固定 Tick、加速、抵達、折返與上行位置。
- 多組參數的距離／時間守恆與路線邊界驗證。

### V2.1.0（29 項）

- 固定 `0.1 s` 子步進、Jerk 限制、平順抵站、不越站與惰行。
- 里程速限重疊取最低、方向分離、10 m 精度、範圍與提前煞車。
- 同方向同軌相鄰配對、反應時間與安全距離、移動閉塞控制。
- 障礙物急停、排程、碰撞防護、營運／緊急煞車切換。
- 車輛／車次身分分離、CSV 跨日、軌跡降採樣與首班準時啟用。
- 終點長折返占用期間完整動態安全距離不被侵入，且沒有碰撞事件。
- 極短班距在起點未淨空時延後發車，同一碰撞只記錄一次事件。
- 版本化專案 JSON 完整往返，破損 JSON 與未知版本會被拒絕。
- 動態 Jerk 煞車包絡線使中間站與終點在到站判定前先降至接近零速，不再由約 20～30 km/h 單 Tick 歸零。
- 移動閉塞允許速度沿用相同煞停包絡線；高速列車不再因移動授權直接硬停。
- 普通停站、跨站不停車、車站通過速限、折返後更換模式及 schema 1 舊存檔升級為全停站模式。
- 三段式軟體版本格式、Engine 組件版本與畫面顯示來源一致。

### 缺陷重現與修正比較

| 情境 | 修正前 | 修正後 |
|---|---|---|
| 預設六站中間站／終點抵達前一 Tick | 約 28.49／28.73 km/h，下一 Tick 直接為 0 | 約 0.83／0.63 km/h，再依位置與低速容許值判定到站 |
| 移動閉塞控制高速硬停 | 診斷案例各出現 10～11 次單 Tick 高速歸零 | 0 次；無碰撞、無負淨距 |
| 跨站列車通過未停靠站 | 仍受全域 30 km/h 進站值影響 | 不套用停站曲線；只受列車性能、路線／車站通過速限與移動閉塞控制 |

## Windows UI 實際重新驗收

以下不是僅看程式碼，而是從編譯後 Windows WPF 程式實際點擊、播放、切換與儲存檔案：

| 項目 | 結果 | 證據摘要 |
|---|---|---|
| V2.1.0 主畫面 | 通過 | 標題及功能摘要從組件版本自動產生；上方「檔案／設定」選單、左側可捲動設定面板、主要控制與各分頁均可操作 |
| 檔案／設定選單 | 通過 | 「存檔／讀取存檔」只位於上方「檔案」；「設定 → 前往 V2 營運參數」可把左側面板定位到 V2 設定 |
| 專案存檔／讀取 | 通過 | 實際儲存 `project-roundtrip.mrtsim.json`，修改路線名稱後讀回；自動化再驗證 schema 2 可保存服務模式／車次，schema 1 以全停站預設升級 |
| 服務模式 UI | 通過 | 實際新增 `EXPRESS / 快速車 / O02 / 跨站` 模式列與 `Vehicle 01 / 下行 / 普通車 / ALL_STOP` 車次列，表格與新增／刪除控制正常顯示 |
| 預設防追撞模式 | 通過 | 新啟動與示範資料均顯示「控制（防追撞）」；「監視」明確標示為「只告警」 |
| 六站示範建模 | 通過 | 6 站、6.2 km、單程 8分35秒、循環 26分10秒、基準班距 4分22秒、最高／峰值 80／80 km/h |
| V2 初始發車 | 通過 | 06:00:00 建模後 V01 已啟用，其餘車輛保持待發 |
| 實際營運播放 | 通過 | 以 20× 播放至 06:02:17.9；V01 位於 1.50 km、44.3 km/h、惰行階段 |
| 里程速限 UI | 通過 | 顯示 `1.25～1.87 km，45 km/h，雙向` 與 `3.30～4.20 km，55 km/h，下行` |
| 路線與速度圖 | 通過 | 雙軌、車輛位置、速限區間、速度及相位隨 Engine 更新 |
| 移動閉塞與煞車頁 | 通過 | 營運／緊急估算、配對／方向／狀態／時間篩選、障礙排程與安全圖表控制可見可操作 |
| 終點占用安全間距 | 通過 | 120× 播放跨越多次終點折返；全程最低安全裕度 0.0 m，終點占用配對實際淨距 31.1 m、動態安全距離 31.0 m、裕度 +0.1 m，無負安全裕度或追撞 |
| 障礙物急停 | 通過 | 於 06:02:17.9 立即停止 V01；事件表保留車輛、1.50 km 與「保守測試事件」說明 |
| 時間－里程運行圖 | 通過 | 計畫虛線與實際實線並列，事件點與事件表同步 |
| 匯出控制 | 通過 | 方向、車輛、計畫／實際、縮放、時間範圍、高解析 PNG、A4／A3、PDF 分頁均可操作 |

安全間距實機驗收使用六列車示範資料並播放至 06:43:09.8；安全頁全程最低裕度為 0.0 m。另以二站、三列車、25 秒班距、終點折返 180 秒的自動化案例專門覆蓋高風險終點占用情境。

## 匯出檔案實際驗收

驗收目錄：`release/ui-export-test`

| 檔案 | 結果 | 檢查 |
|---|---|---|
| `diagram.png` | 通過 | 27,830 bytes，875×380，畫面文字與軌跡可讀 |
| `diagram.pdf` | 通過 | 95,572 bytes，`%PDF-1.4`，A4 橫向分頁共 2 個 page object |
| `trajectory.csv` | 通過 | 115,839 bytes；欄首含 vehicle／service run／時間／里程／速度／方向／軌道／狀態／前後站／事件；新版另加入 service class／service pattern |

CSV 首筆實際資料為 V01／R01-D001、06:00:00.1、Outbound、DOWN、Accelerating，與 UI 首班發車狀態一致。

## 驗收界線

本次完成的是 V1.0 相容核心，加上 V2.1.0 實際營運軌跡、動態進站煞車、停站／跨站服務模式、里程速限、概念性移動閉塞、障礙測試及運行圖匯出。以下仍未完成，不能宣稱已驗證：

- 特定真實路線的坡度、曲率、超高、黏著、車型與時刻資料校準。
- 共用單線、交叉渡線、道岔、聯鎖與通訊／列車完整性失效模型。
- ATP／ATO／ATS 或安全完整性等級認證。
- 實體列車、號誌設備或營運現場驗收。

因此本軟體僅供營運與號誌概念模擬，不可作為真實鐵路安全決策或部署依據。
