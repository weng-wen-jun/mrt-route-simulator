# 路線與站場建置規則

本表是軟體建置契約，適用快速起稿、站場編輯、專案讀取及範例維護。軌道位置以 edge-local offset 為準；示意圖不得反向改寫模擬位置。

## 實體接軌側別

`FromPortSide`／`ToPortSide` 為每個節點局部的 A/B 兩側。列車行進接續須由一側進、另一側出；同 edge 停車換端仍由既有折返與全車占用規則處理。`CONNECTION-001` 拒絕同側轉向或已知／未知側別混接，`CONNECTION-002` 拒絕缺一端或非法值。此資料不隨示意座標移動而重算，編輯器、讀寫檔、套用及尋徑共用相同規則。

新站型及快速建線提供側別，分割及設施精靈保留它；尾軌／袋狀軌精靈另產生各25m的實體進出渡線，尾軌長度欄位仍指止衝前主體長度。舊檔兩端皆未填時保留相容性，不代表方向已驗證；後續遷移見 TODO。

## 里程與停車基準

路線圖以主下行進路起始站的月台中心為 **0.000K**，起站外側尾軌顯示負里程；終點站中心為 K，外側軌道顯示 K 加延伸距離。上下行依同站中心校準。此為路線圖及即時車體中心欄位的里程；模擬內部仍使用各軌道自己的非負 offset，既有進路距離統計不改成示意里程。

新建 PDF 站型的 `StopPositionReference=TrainCenter`：停點表示車體中心，Engine 依行車方向及車長解析車頭位置。站內換端保持整列車的占用範圍不動，由原車尾成為新車頭。舊檔省略此欄位時仍為 `TrainFront`，不擅自平移既有車頭停車標；編輯器可明確選擇基準。

| 規則 | 處理方式 |
|---|---|
| STATION-001：同站已填寫的月台編號不得重複（忽略大小寫及首尾空白） | 拒絕套用、存檔與建立 runtime；空白保留既有自動編號相容性 |
| STATION-002：有效長度不得大於非零實體月台範圍 | 拒絕資料驗證 |
| STATION-003：車頭停點後方及有效長度須容納實際車型 | runtime建立／編輯套用前依派車、停站模式與方向檢查；不是只比較月台總長 |
| TURN-001：同 edge 反向 traversal 的 Crossover 站內折返，到站、折返、出發的中心錨點必須相同 | 車頭位置由方向及車長解析，不要求換端前後車頭 offset 相同 |
| TURN-002：上述站內折返必須對齊實體月台停點，且停點位於月台範圍內 | 錯誤指出設施 ID，要求同步修改 offset |
| TURN-003：到達進路末站與出發進路首站必須包含上述停點的同一月台 | 拒絕錯接月台或跳站的折返作業 |
| TURN-004：站內原地折返月台須採車體中心定位 | 拒絕以同一車頭停點換端導致整車位移 |
| TURN-005：折返停點前須有足以涵蓋整車的反向返回進路 | 依實際車型驗證；不能在車尾尚留於無反向返回路徑的渡線時換端 |
| 有向進路連續、edge 方向、參照及 offset 範圍 | 沿用 InfrastructureValidator；共用節點不代表可跨股道 |
| 站名中心由實際繪製的月台本體取得 | 同中心共用站名，錯列本體各自標示及引線；不假移軌道來對齊 |
| 站名偏移超過 0.5 px、互相重疊、超出圖面、文字截斷、月台號碼重疊 | 從真正Rectangle檢核並顯示「版面檢核」提示，滑鼠停留查看明細 |
| 編輯器設施圖例 | 自站名下方排列，圖面隨數量增高並可捲動 |
| 載入候選資料 | 計畫時間軸、雙向速度預覽及站列先準備成功，再替換目前專案／會話 |

## 範例與變更驗收

在 Windows 專案根目錄執行：

```powershell
./tests/ValidateStationConstruction.ps1
```

此入口依序執行 Release build、完整 Engine 測試及正式 WPF 測試。WPF 測試自動遞迴枚舉 `samples/*.mrtsim.json`，實際呼叫主視窗載入流程，包含計畫時間軸及雙向速度預覽；任何例外會令測試失敗。所有範例均檢查720／1200 px的主圖及編輯器，並另測大量設施圖例。

新增範例必須加入 samples 並通過上述入口；新增站型須補對應實體運行測試。錯誤案例也必須留在測試中，不能只驗證正常範例。

## 共用位置映射與完整範例回歸（2026-09-12）

月台的最小顯示寬度只能繞實體中心展開，不得為軌道端點留白而平移月台。停靠對位須涵蓋起站、中間站、終點，並在 WPF 完成 Measure／Arrange 後比較實際畫面中心，不只比較 Canvas 屬性。

主圖與編輯器的軌道、月台及列車以相同 edge-local offset → StationChainageProjection → 畫面位置函式呈現，不能只對齊站名字串。同站可以有不同股道及多個月台本體；完整範例的一島一側配置必須共享站中心，不得把放錯軌段的月台以重複里程標籤掩蓋。

完整範例的端點使用站後渡線及尾軌，袋狀軌以進出道岔、單一袋軌及反向返回組成。WPF 回歸逐一確認有向連接端點重合、進路不反折、月台中心對齊，並以實際停站狀態驗證列車對位。這些是明確範例的驗收條件，不是對任意拓撲實施「同站必須同一 edge」限制。

## 邊界

版面提示尚未涵蓋所有軌道碰撞及任意自訂幾何。站型不依名稱推測；TURN規則適用明確的同edge原地反向結構，不把尾軌折返強制拉回月台。月台中段尾軌銜接僅在停點吻合且有向端點連續時允許，列車須走完剩餘到達軌道並實際返回出發停點。欄位尚未自動聯動所有offset。零範圍月台保留legacy相容，未通過實體容車檢核；正式範例不可用零範圍迴避檢核。

WPF 自動化使用 STA 程序內的真實控制項，不等同桌面檔案選擇對話框人工驗收。失敗保留測試涵蓋無效專案；準備完成後純 UI 繪圖的非預期例外尚無完整 rollback 保證。
