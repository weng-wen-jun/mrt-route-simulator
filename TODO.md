# 待修正／改善清單

## 介面修正（2026-08-20，已完成並驗收）

- [x] 在主視窗上方新增傳統選單列，位置依參考圖，位於視窗標頭區下方、主要內容上方。
- [x] 新增「檔案」選單，將原左側的「存檔」與「讀取存檔」移入；沿用 `.mrtsim.json`、讀取驗證與原子寫入行為。
- [x] 移除左側重複的「存檔／讀取存檔」按鈕，避免同一功能出現兩個入口。
- [x] 在「檔案」旁新增頂層「設定」，提供「前往 V2 營運參數」入口。
- [x] 驗收：滑鼠可操作；鍵盤以 `F10`、方向鍵展開「檔案」並選取項目；存檔／讀取回歸測試通過，1426×893 視窗下選單未重疊或消失。

## 進站煞車與車次停站模式修正（2026-08-20，已完成並驗收）

已以預設參數重現：中間站前一個 Tick 距停車點約 `0.04 m` 時仍為 `28.49 km/h`，終點前約 `0.39 m` 時仍為 `28.73 km/h`，下一個 `0.1 s` Tick 便直接歸零。這是引擎實際速度不連續，不是單純的畫面更新問題。

> 修正結果：相同案例的中間站／終點到站前速度已降為約 `0.83／0.63 km/h`，並新增到站前一 Tick 及高速硬停回歸測試。

### A. 動態煞停距離與平順抵站

- [x] 統一煞車曲線與實際控制所用的減速度；不再使用估算與施加不一致的進站減速度折減係數。
- [x] 每個固定 `0.1 s` Tick 依目前速度、目前加速度、Jerk、實際營運煞車能力與剩餘站距，向前積分至速度為 0，計算當下實際所需的煞停距離。
- [x] 當剩餘站距進入動態煞停距離（含一個 Tick 前視量）時開始煞車，並持續更新停止包絡線。
- [x] 到站判定同時要求距停車點不超過 `0.5 m` 且速度不高於 `0.15 m/s`；不再因跨過站位就把高速直接設成 0。
- [x] 現有性能與剩餘距離無法正常煞停時，保持煞車並留下 `StationStopViolation` 事件，不以無條件歸零掩蓋。

### B. 停站、跨站與高等級列車

> 術語統一：「車型」指列車硬體與物理性能；「服務等級」指普通、快速、直達等營運分類；「停站模式」指各站停靠／通過規則。後續一律使用「車型」，也不得把服務等級誤當成物理車型。

- [x] 原「進站控制速度 30 km/h」改為「停站進站上限」；預設 `0` 由動態煞車包絡線決定，正值才是停站車額外上限。
- [x] 新增車次停站表，支援每個方向別車次、每一車站的「停站／跨站」設定；停站列車以月台停車點 `0 km/h` 為最終目標。
- [x] 跨站／高等級列車通過未停靠車站時不套用停站曲線；仍遵守路線速限、車站通過速限、列車性能及移動閉塞保護。
- [x] 實體列車折返後的新車次可採用不同列車等級與停站模式；車輛 ID 與車次設定分離。
- [x] schema 2 專案存檔／讀取保存列車等級、各車次停站表及車站通過速限；schema 1 舊存檔自動升級並預設 `普通車 / ALL_STOP`。

### C. 回歸驗收

- [x] 新增預設參數重現案例，確認中間站與終點到站前速度已連續降至接近 0，不再發生約 20～30 km/h 單 Tick 歸零。
- [x] 驗證每一 Tick 的速度變化符合實際加速度、Jerk 與時間步長，並保留既有短／長區間、惰行與不同控制條件案例。
- [x] 以同一路線、同一車站驗收普通停站車平順停車，以及跨站／高等級列車依允許速度安全通過。
- [x] 重新執行多列車、終點占用與防追撞回歸測試；移動閉塞控制高速硬停由診斷案例的 10～11 次降為 0，無碰撞或負淨距。

## 端點折返、站場軌道與月台配置（2026-08-20，待實作）

本項依參考圖整理需求。圖中的 `100 m`、`30 s`、`40／60／80 km/h`、`1.50` 等數值只作為可輸入參數範例，不直接寫成固定預設值。

### A. 端點站折返形式

- [ ] 起點與終點須可分別設定折返形式，第一階段至少支援「站前折返」及「站後折返」，不得假設兩端使用相同形式。
- [ ] 「站前折返」須建立站前橫渡線、進路及衝突區；列車通過橫渡線後進入指定月台股道，完成停站／折返後由相容進路反向發車。
- [ ] 「站後折返」須建立月台後方橫渡線、尾軌與停車位置；列車抵站後駛入尾軌，完成尾軌折返時間，再返回指定發車月台或直接進入反向正線。
- [ ] 支援站內或尾軌交替使用不同股道；若未啟用交替，必須能指定固定抵達股道、折返股道及發車股道。
- [ ] 折返設定至少包含：名稱、形式、進出站坡度（‰）、月台停車點至橫渡線距離、橫渡線長度、橫渡線至尾軌停車點距離、站內／尾軌折返時間、道岔或橫渡線速限、進入折返區前進路速度及安全係數。
- [ ] 所有距離須轉換為明確的累積里程及占用區段；坡度、速限與折返時間須實際進入運行時間與速度控制，不得只保存於 UI。
- [ ] 折返進路須有預約、鎖定、占用與釋放狀態；互相衝突的進路不得同時開通，前車未淨空橫渡線或尾軌時後車不得侵入。
- [ ] 圖中另有「中央避車線折返」形式；先列為候選擴充，實作前確認是否與第一階段一併納入，未確認前不得自行視為必做範圍。

### B. 車站軌道及月台配置

- [ ] 每站新增站場配置，至少包含月台數、正線股道數、月台股道數、通過／待避股道數，以及各股道的方向、有效長度、停車點、月台對應與速限。
- [ ] 月台形式至少預留：側式月台、島式月台、雙島式四股道、側式月台加中央通過線，以及無月台的通過／待避線；UI 以配置選單與簡化線形預覽呈現。
- [ ] 每一月台須能設定服務方向、可停靠車型與服務等級、有效長度、停站位置及是否允許載客服務；列車長度超過有效長度時須阻止建模並顯示中文原因。
- [ ] 每一股道須有獨立 `TrackId`、占用狀態與可用進路；現有固定 `DOWN／UP` 雙軌假設須改為依列車實際所在股道配對與保護。
- [ ] 車站速限須區分正線、月台股道、通過線、橫渡線及道岔；高等列車跨站時依實際選定股道套用相應速限。
- [ ] 站場配置與列車運行圖、路線動畫、事件紀錄及 CSV 匯出須使用同一套股道與月台資料，不得各自推測配置。

### C. 高等列車追越／待避規則

- [ ] 每站可設定是否具備待避／追越能力；只有存在兩條以上相容進路，且其中一條能讓普通車安全停等時，才可啟用追越。
- [ ] 追越設定至少包含：普通車待避股道、高等列車通過股道、適用方向、優先等級、最短待避時間、進路建立／解除時間及安全餘裕。
- [ ] 普通車進入待避股道並完成停車及進路鎖定後，高等列車才可由通過股道追越；高等列車完全淨空衝突區後，普通車才能重新取得發車進路。
- [ ] 若實際到站順序、停站時間或安全間距不足以完成追越，系統須延後或取消追越並留下事件，不得強迫兩列車交換順序或瞬間換軌。
- [ ] 移動閉塞相鄰配對須隨股道與進路切換更新；不同股道且無衝突進路的列車不互相阻擋，匯入同一股道或共用道岔時則須納入衝突保護。
- [ ] UI 須顯示目前月台／股道占用、預約進路、等待原因及追越結果；運行圖須標記進入待避、追越完成、追越取消及進路衝突事件。

### D. 存檔、相容性與驗收

- [ ] 專案格式預計升級為 schema 3，完整保存兩端折返形式、站場配置、月台、股道、進路與追越規則；schema 2 舊檔載入後須維持既有雙軌、無追越及抽象站前折返的運行結果。
- [ ] 驗證站場資料：股道／月台 ID 不重複、距離與有效長度為有限正數、坡度及速限在合理範圍、進路端點存在、方向相容，且無自相矛盾的股道連接。
- [ ] 分別驗收站前折返與站後折返的完整軌跡、折返時間、進路占用、終點排隊及反向發車；尾軌被占用時後車必須安全停等。
- [ ] 至少以二股道側式月台、二股道島式月台、四股道待避站三種配置驗收停站、跨站及雙向運行。
- [ ] 以普通車待避、高等列車追越案例驗證股道分配、通過速限、進路衝突、先後順序與安全間距；另覆蓋空間／時間不足而取消追越的案例。
- [ ] 補齊 Engine 自動化測試、schema 2→3 升級測試、Windows UI 操作與長時間多列車回歸；既有 53 項測試必須持續通過。

### E. 實作前待確認範圍

- [ ] 確認「中央避車線折返」是否與站前／站後折返同批實作。
- [ ] 確認參考圖中的「銜接點／主線與支線」是否另立需求；目前不把支線分岔及列車分流比例納入本項。
- [ ] 確認追越採使用者預先指定車站與車次，或由模擬器依誤點及可用進路動態決策；兩者會影響 UI、排程與驗收方式。

## 區間物理明細新增 V2 實際運行統計（2026-08-20，待實作）

目前「區間物理明細」只顯示 V1 解析模型的下行三角形／梯形理論結果，尚未統計 `SimulationWorld` 實際受到 Jerk、惰行、里程速限、停站／跨站模式及移動閉塞控制後的區間表現。

### A. 資料定義與統計口徑

- [ ] 新增顯示模式：「V1 理論」、「V2 實際」及「理論／實際比較」，不得用 V1 計算值冒充 V2 結果。
- [ ] V2 區間資料須直接由 `TrajectorySample` 與 `SimulationEvent` 統計；區間定義為列車車頭離開前站停車點至到達或通過後站停車點，不含後站停站時間。
- [ ] 每筆實際區間須保留車輛 ID、車次 ID、車型、服務等級、停站模式、方向、前後站、起訖模擬時間及是否為停站／跨站。
- [ ] 下行與上行須分開統計；上行里程遞減時仍依實際行車方向正確判定區間起訖，不得把站序或時間顛倒。
- [ ] 車站通過時間與區間邊界應在相鄰 `0.1 s` 取樣間依位置內插，避免直接取最近 Tick 造成固定 `0.1 s` 誤差；事件已有精確時間時優先使用事件。
- [ ] 尚未完成的區間須標示「運行中」並與完整區間分開，不得混入平均值、百分位數或完成區間比較。

### B. 每車次區間明細

- [ ] 每一完成區間至少顯示：實際距離、實際旅行時間、平均速度、峰值速度、進入速度、離開速度及區間最低有效速限。
- [ ] 統計 V2 相位時間：加速、巡航、惰行、減速，以及受到移動閉塞／進路控制限制的時間；各相位合計須與實際區間旅行時間在容許誤差內一致。
- [ ] 顯示控制介入摘要：提前速限煞車、停站煞車、移動閉塞制動、等待移動授權及異常停站事件的次數或累積時間。
- [ ] 在比較模式顯示 V1 理論與 V2 實際的旅行時間差、百分比差、峰值速度差及主要延誤來源；跨站車只與相同停站條件的基準比較。
- [ ] 若目前模型缺少列車質量、阻力、坡度與牽引／再生效率，不得顯示虛假的能耗數值；能耗統計另待相關物理參數建模後新增。

### C. 多車次彙總與 UI

- [ ] 提供方向、車輛、車次、車型、服務等級、停站模式及模擬時間範圍篩選；篩選只改變顯示與統計，不得修改模擬資料。
- [ ] 支援「逐車次明細」及「區間彙總」；彙總至少顯示完成樣本數、平均、最小、最大及第 95 百分位旅行時間，樣本不足時須清楚標示。
- [ ] 原有 V1 欄位完整保留；V2 欄位較多時可使用第二張表或欄位群組，避免主表橫向過度擁擠。
- [ ] 在表格上方顯示目前統計來源、模擬模式、取樣起訖時間及有效樣本數；重設或重新建模後必須清除舊統計。
- [ ] 支援將目前篩選後的 V2 區間明細與彙總結果匯出 CSV，欄位單位與畫面一致，取消匯出不得留下不完整檔案。

### D. 驗收條件

- [ ] 以無速限單列車案例確認 V2 實際時間與相位加總一致，並能合理接近無干擾基準；容許誤差須明確寫入測試。
- [ ] 以惰行、區間速限、跨站、移動閉塞制動及終點占用案例，確認實際旅行時間與延誤來源會正確改變。
- [ ] 驗證上下行、折返後新車次、跨午夜、未完成區間、零停站時間及多次循環，不產生重複、漏算或跨車次串接。
- [ ] 彙總值須由逐車次明細重新計算得到；平均、極值及第 95 百分位需有自動化數值測試。
- [ ] UI 顯示、CSV、運行圖及 Engine 查詢的同一車次區間時間須在容許誤差內一致。
- [ ] 補齊 Engine 統計、自動化回歸與 Windows UI 操作驗收；既有 53 項測試必須持續通過。

本項屬新增功能；實際完成並正式進版時，依 `VERSIONING.md` 增加次版本，未實作前維持目前 `V2.1.0`。

## 新增明確的模擬模式切換（V1／V2）（2026-08-20，待實作）

目前已有位於「V2 營運物理」區內的「基礎物理模式（V1）／實際營運模式（V2）」下拉選單，Engine 也能在 V1 `SimulationEngine` 與 V2 `SimulationWorld` 間選擇；但入口不夠明顯，且不適用的參數、分頁與舊結果沒有隨模式完整切換，容易讓使用者誤判目前實際使用的模型。

### A. 全域切換入口與模式定義

- [ ] 在主畫面上方或「設定」選單提供醒目的全域「模擬模式」切換，選項固定為「V1 基礎物理」及「V2 實際營運」；原 V2 參數區內的重複入口須移除或改為同步顯示。
- [ ] V1 模式只使用既有 `TripSimulator`／`SimulationEngine` 的三角形／梯形解析模型，不建立或偷偷套用 V2 `SimulationWorld`、Jerk、惰行、里程速限、服務模式或移動閉塞控制。
- [ ] V2 模式使用 `SimulationWorld` 固定 `0.1 s` 子步進，並套用 Jerk、惰行、動態煞停、里程速限、停站／跨站服務模式、移動閉塞與事件。
- [ ] 畫面標題區、狀態列、播放區及各輸出須清楚標示目前是 V1 或 V2；不得只用「基礎／實際」等不含版本的模糊文字。
- [ ] 「V1／V2 模擬模式」是本次執行所用 Engine；「理論／實際比較」只是結果檢視方式，兩者不得混為同一個設定。

### B. 參數、功能與畫面狀態

- [ ] 選擇 V1 時，V2 專用參數、速限、服務模式、移動閉塞、障礙物急停及 V2 匯出控制須停用或收合，並顯示「切換至 V2 後可用」；使用者已輸入的 V2 數值保留但不得生效。
- [ ] 選擇 V2 時重新啟用 V2 專用區塊；V1 共用的路線、車站、列車性能、列車數、班距及首班時間沿用，不要求重複輸入。
- [ ] 各模式保留自己的播放時間、篩選條件及結果狀態；若為降低複雜度而不保留，切換時至少須明確清除舊結果並說明需重新建立模擬。
- [ ] 模式變更後不得繼續播放切換前的世界；停止計時器、清除不相容的即時狀態與圖表，將「播放」停用至重新按下「計算並建立模擬」。
- [ ] 已修改但尚未重新建模時，狀態列須顯示「模式已變更，請重新建立模擬」，避免把舊結果誤認為新模式結果。
- [ ] V1 不適用的分頁可保留但須呈現清楚空狀態；不得顯示上一次 V2 的安全距離、事件或實際運行圖。

### C. 存檔、輸出與版本相容

- [ ] `.mrtsim.json` 須保存最後選擇的 V1／V2 模式；讀取後恢復模式與相應 UI 狀態，但不恢復播放到一半的瞬時世界。
- [ ] 舊存檔缺少明確模式欄位時，依既有 `profileMode` 移轉；仍無法判定時採目前相容預設並在讀取訊息說明。
- [ ] CSV、PNG、PDF、時刻表及區間物理明細須附上模型版本；V1 輸出不得出現 V2 控制事件，V2 輸出不得標示成 V1 解析結果。
- [ ] V2 的計畫／理論比較世界可在背景使用 V1／無干擾基準，但輸出必須標明「比較基準」，不能把目前執行模式改顯示為 V1。

### D. 驗收條件

- [ ] 以同一路線分別建立 V1 與 V2，確認 V1 結果保持既有 24 項解析相容性，V2 則確實出現 Jerk／惰行／速限等差異。
- [ ] 在播放前、播放中、暫停及完成後切換模式，均不發生計時器重複、舊世界繼續推進、圖表殘留或資料混接。
- [ ] 驗證切換 V1→V2→V1 後共用輸入與各模式專用輸入保留正確，且每次都必須重新建模才可播放。
- [ ] 驗證存檔／讀取、schema 1／2 相容、取消讀取及損壞檔案情境，不會意外改變目前模式或現有設定。
- [ ] 完成 Windows UI 的滑鼠與鍵盤操作驗收，並補齊模式切換、控制啟停、結果清除、輸出標記及回歸測試；既有 53 項測試必須持續通過。

本項屬新增功能；若與其他待修新功能併入同一次正式進版，依 `VERSIONING.md` 只增加一次次版本，不重複累加版本號。

## 新增雙向同時發車功能（2026-08-20，待實作）

目前 V1 `SimulationEngine` 與 V2 `SimulationWorld` 都把全部初始列車配置在路線起點，依班距以下行方向發車；上行列車只能由下行列車抵達終點並完成折返後產生。本項須支援模擬開始時由路線兩端分別配置列車並同時或依各自時刻發車。

### A. 發車設定與定義

- [ ] 新增「雙向初始發車」開關；關閉時完整保留目前全部列車由起點下行發車的相容行為。
- [ ] 開啟後可分別設定下行與上行的初始列車數、首班發車時間及班距；兩方向首班時間相同時，須能在同一模擬時間精確發車。
- [ ] UI 預設採「上下行列車數分開輸入」，並顯示總車輛數，避免單一「列車數量」究竟是每方向或全線總數的歧義。
- [ ] 可提供「同步首班」快捷選項，把上行首班時間同步為下行首班時間；取消同步後仍可設定不同時間，不應限制只能完全同時。
- [ ] 首班時間、班距及列車數須有方向別中文驗證；任一方向可為 0 列，但兩方向合計仍須至少 1 列。
- [ ] 班距留空時須明確定義方向別自動班距；不得沿用全線總車數直接除以循環時間而造成兩方向班距錯算。

### B. Engine 排程與列車身分

- [ ] V1 與 V2 使用同一組方向別初始發車計畫資料結構；V1 產生解析班表，V2 建立有狀態列車，不得由 UI 各自拼湊兩套排程。
- [ ] 下行初始列車位於第一站、方向為 `Outbound`、軌道為 `DOWN`；上行初始列車位於末站、方向為 `Inbound`、軌道為 `UP`，位置與車身範圍不得超出路線邊界。
- [ ] 每一實體車輛須有全線唯一 `VehicleId`；方向別初始車次須有唯一 `ServiceRunId`，不得因上下行序號相同而覆蓋事件、軌跡或服務模式。
- [ ] 方向別 `ServiceRunPlan` 須在列車首次發車前套用正確的車型、服務等級與停站模式；未指定時仍採預設車型及 `普通車 / ALL_STOP`。
- [ ] 兩端發車須各自執行起點淨空與移動閉塞檢查；某一端因前車、折返列車或站場進路占用而延後時，不應無故阻擋另一端安全發車。
- [ ] 初始上行列車抵達第一站折返、初始下行列車抵達末站折返後，均繼續使用同一實體車輛進入下一車次；不得再次建立重複車輛或重複套用初始發車排程。
- [ ] 同一 Tick 內有多筆方向別發車事件時，事件處理順序須可重現，且不因集合順序不同而改變物理結果。

### C. UI、統計與輸出

- [ ] 即時列車狀態在模擬開始後須同時看到兩端已啟用列車，方向、位置、下一站及車次資料正確。
- [ ] 時刻表、區間物理明細、V2 實際統計、移動閉塞頁與時間－里程運行圖均須包含初始上行及下行資料，不得只顯示下行首批列車。
- [ ] 運行圖在相同首班時間須由路線兩端同時出現兩條反向軌跡；上下行使用既有不同顏色／線型並保留跨午夜連續時間。
- [ ] 摘要須分別顯示上下行列車數、首班時間、班距及有效發車數；全線總車數不得與單一方向數量混淆。
- [ ] CSV、事件及圖檔輸出須保留方向別初始發車時間、車輛／車次 ID 與軌道；同一時間的事件不得遺失或互相覆蓋。

### D. 存檔與相容性

- [ ] `.mrtsim.json` 保存雙向發車開關、兩方向列車數、首班時間及班距；讀取後須還原 UI，但仍需重新建立模擬。
- [ ] schema 1／2 舊存檔沒有方向別發車設定時，預設關閉雙向初始發車並沿用原有下行排程，確保舊檔結果不被改變。
- [ ] 若本功能與站前／站後折返、月台股道配置一併完成，方向別發車須使用相應月台與進路；站場模型尚未完成前則維持既有 `DOWN／UP` 獨立雙軌假設並清楚標示。

### E. 驗收條件

- [ ] 兩站路線在 `00:00:00.0` 同時發出一列下行與一列上行；第一個 V2 Tick 前兩車均已啟用，且各有一筆時間完全相同、方向正確的發車事件。
- [ ] 驗證不同方向列車數、不同班距、不同首班時間、單方向 0 列與跨午夜首班，不產生遺漏、重複或負時間。
- [ ] 驗證多列車雙向運行與多次折返後，實體車輛總數不增加、ID 不重複、車次方向交替正確且軌跡連續。
- [ ] 在移動閉塞控制模式驗證兩方向各自只與同方向同軌前車配對；既有獨立雙軌下，反向列車不得互相誤判為追撞對象。
- [ ] 驗證一端因起點未淨空而延後、另一端仍準時發車，以及兩端都被占用時各自等待的情境。
- [ ] 驗證 V1／V2 模式切換、存檔往返、時刻表、區間統計、運行圖及 CSV 的雙向資料一致性。
- [ ] 補齊 Engine、自動化回歸及 Windows UI 操作驗收；既有 53 項測試必須持續通過。

本項屬新增功能；若與其他待修項目合併為同一次正式進版，依 `VERSIONING.md` 只增加一次次版本。

## 頂端「輸入」選單、車型設定與中文停站模式（2026-08-20，待實作）

目前車型性能、停站／跨站模式及車次套用資料集中在左側 V2 參數長表，且畫面直接顯示 `ALL_STOP`、`EXPRESS`、模式 ID 等英文或技術代碼。本項須重新整理為頂端輸入流程，讓使用者以中文名稱完成車型及停站模式設定。

### A. 頂端「輸入」入口

- [ ] 在主視窗頂端選單新增「輸入」，位置固定在「檔案」與「設定」之間，順序為「檔案｜輸入｜設定」。
- [ ] 「輸入」選單至少包含「車型設定」與「停站模式設定」兩個項目；不得把兩者合併成語意不明的「列車設定」。
- [ ] 點選「車型設定」開啟專用設定畫面；點選「停站模式設定」開啟另一個專用設定畫面，完成後回到主畫面並更新所有相關下拉選單。
- [ ] 原左側 V2 長表中的車型與停站模式編輯區須移除或改為只讀摘要，避免同一資料有兩個可編輯入口。
- [ ] 支援滑鼠與鍵盤操作；`F10` 聚焦主選單後可用方向鍵進入「輸入」，選單項目須有中文 Automation Name 與可見焦點。
- [ ] 關閉或取消設定畫面時不套用未確認的修改；確認後若既有模擬已建立，須標示「輸入已變更，請重新建立模擬」。

### B. 車型設定

- [ ] 車型使用中文顯示名稱，例如「標準型電聯車」、「高運量電聯車」；內部使用不可變的 `VehicleTypeId`，但一般 UI 不顯示技術 ID。
- [ ] 每一車型至少設定：名稱、車長、最高速度、加速度、一般減速度、營運煞車、緊急煞車、Jerk 及牽引力遞減／惰行相關參數；欄位單位與中文驗證須清楚。
- [ ] 目前全域列車性能在舊檔移轉時建立為「預設車型」，維持既有模擬數值；不得因新增車型結構改變舊檔運行結果。
- [ ] 車次服務計畫的「車型」欄改為下拉式選單，直接列出已建立車型的中文名稱；實際保存 `VehicleTypeId`，改名後既有車次關聯仍有效。
- [ ] 刪除仍被車次、月台相容規則或存檔設定引用的車型時須阻止刪除，或要求先指定替代車型；不得留下失效下拉值。
- [ ] 「車型」與「服務等級」保持分離：車型控制物理性能，普通／快速／直達屬服務等級，不得以車型名稱推斷停站方式。

### C. 停站模式全面中文化

- [ ] 所有使用者可見文字統一使用「停站模式」，不再交替顯示「服務模式」、「模式 ID」或其他技術名稱。
- [ ] 系統預設模式在畫面顯示為「全部停站」；快速或跨站範例使用「快速停站」等中文名稱，不得顯示 `ALL_STOP`、`EXPRESS`、`Stop`、`Pass`。
- [ ] 每站行為下拉選單固定顯示「停站」與「跨站」；通過速度欄使用「通過上限（km/h）」等完整中文名稱。
- [ ] 停站模式設定畫面以中文名稱作為主要識別，顯示各站編號、站名、停站／跨站及通過上限；內部穩定 ID 自動產生或隱藏，不要求使用者手動輸入英文代碼。
- [ ] 車次服務計畫的「停站模式」欄改為下拉式選單，列出已建立模式的中文名稱；新增、改名或刪除模式後立即更新。
- [ ] 刪除仍被車次引用的停站模式時須阻止刪除或要求重新指定；系統內建「全部停站」不得刪除。
- [ ] 狀態列、驗證訊息、事件表、工具提示、CSV 欄名及圖例中的停站模式文字皆須中文化；不得只改主表標題而保留其他英文顯示。

### D. 舊存檔及內部相容性

- [ ] 為維持 schema 1／2 舊檔相容，內部可保留 `ALL_STOP`、`EXPRESS` 等既有 ID，但必須透過中文顯示名稱映射，且一般 UI、錯誤訊息與輸出不得暴露英文 ID。
- [ ] 舊檔的 `ALL_STOP` 自動顯示為「全部停站」；已存在的自訂英文模式名稱應保留資料並允許使用者改成中文，不能無聲合併或遺失引用。
- [ ] 存檔須分離保存不可變 ID 與中文顯示名稱；重新命名車型或停站模式不應破壞車次、折返方向及歷史軌跡的關聯。
- [ ] 若需要升級 schema，須提供舊版移轉測試；若只增加可選欄位即可相容，仍須驗證缺欄位時的中文預設名稱。

### E. 驗收條件

- [ ] 實際從頂端「輸入」分別建立兩種車型與兩種停站模式，回到主畫面後車次表的兩個下拉選單立即出現正確中文選項。
- [ ] 驗證車型改名、停站模式改名、取消編輯、刪除被引用項目及重新指定後，畫面、Engine 與存檔關聯一致。
- [ ] 全面搜尋並操作主畫面、設定畫面、驗證訊息、事件、工具提示與匯出內容，確認使用者可見的停站模式不再出現 `ALL_STOP`、`EXPRESS`、`Stop`、`Pass` 或「模式 ID」。
- [ ] 讀取 schema 1／2 舊存檔後，預設模式顯示「全部停站」、物理參數建立為「預設車型」，重新建模結果維持相容。
- [ ] 完成存檔往返、自動化回歸及 Windows UI 的滑鼠／鍵盤驗收；既有 53 項測試必須持續通過。

本項屬新增功能及介面重整；若與其他待修項目併入同一次正式進版，依 `VERSIONING.md` 只增加一次次版本。

## V2.1.0 實作狀態（2026-08-20）

本文件原始細項保留作為需求與後續追蹤基準。V2.1.0 本次已完成並驗收的交付範圍如下：

- [x] 保留 V1.0 解析模型及 24 項既有測試，新增獨立 V2 `SimulationWorld`。
- [x] 實際營運模式：固定 `0.1 s` Tick、Jerk、牽引力遞減、惰行、動態煞車包絡線與平順抵站。
- [x] 任意里程區間速限：10 m 精度、跨站、重疊取最低、方向別套用、提前煞車、UI 新增／刪除與中文驗證。
- [x] 多列車相鄰配對：車頭／車尾、方向、軌道、車輛 ID 與車次 ID 分離。
- [x] 移動閉塞獨立／監視／控制模式，以及安全、警戒、需要制動、侵入安全距離四種狀態。
- [x] 營運／緊急煞車估算切換、立即／排程障礙物急停、預估停止里程、預測碰撞與不穿越保護。
- [x] 時間－里程運行圖：計畫／理論與模擬實際、方向／車輛／時間篩選、縮放、事件標記與滑鼠提示。
- [x] PNG、PDF（A4／A3、橫向、分頁）及 CSV 軌跡／事件匯出。
- [x] 版本化專案存檔／讀取，完整保存可編輯設定，讀取前驗證並以原子方式寫檔。
- [x] 上方「檔案／設定」選單、停站／跨站服務模式、列車等級、車站通過速限及折返後方向別模式。
- [x] 補強起點發車淨空、終點占用、Jerk 轉換距離、完整動態安全距離移動授權及單次碰撞事件紀錄。
- [x] 新增 29 項 V2 自動化案例；目前合計 53 項測試。
- [x] 導入 `V主版本.次版本.修訂版本` 規則，以 `Directory.Build.props` 集中控制組件、檔案、UI 與測試版本。
- [x] Windows 實機 UI、播放、速限、障礙事件及三種匯出均已重新操作驗收。

下列原始需求仍屬後續工作，V2.1.0 不宣稱完成：

- [ ] 以特定捷運路線的曲率、坡度、超高、黏著、列車性能與實際運行資料進行校準。
- [ ] 共用單線、交叉渡線、道岔、聯鎖與通訊／列車完整性失效模型。
- [ ] 坡度與黏著條件直接納入煞車包絡線，以及安全認證等級的模型與驗證。
- [ ] 相鄰列車圖的分項曲線開關、局部極值／門檻交越標籤及大資料量效能基準。
- [ ] 運行圖的車站區間／運行狀態篩選、同站與斷面間隔分析，以及獨立時間／里程比例控制。

> 安全界線：V2.1.0 是營運與號誌概念模擬器，不是可部署或經認證的 ATP／ATO／ATS 鐵路安全系統。

## 0. 共同架構與衝突處理原則

以下決策適用於本清單所有項目，後續設計與驗收不得採用互相矛盾的解讀。

- [ ] 完整保留 V1.0 三角形／梯形解析模型及既有公開行為，作為「基礎物理模式」；既有 24 項自動化測試必須持續通過。
- [ ] 新增獨立的 V2 `SimulationWorld`，負責多相位速度軌跡、里程速限、多列車狀態、移動閉塞、事件及碰撞；不得把有狀態控制硬塞入 V1.0 的任意時間解析查詢。
- [ ] 正常運行的速度、加速度與位置必須連續；障礙物急停是明確標記的非物理例外事件，不納入正常連續性驗收。
- [ ] 上下行預設為不同軌道並分別判定相鄰列車；共用軌道、交叉渡線及聯鎖留待另案建模。
- [ ] 尚未發車、已退出營運或不在指定軌道上的列車，不參與移動閉塞相鄰配對。
- [ ] 列車資料須區分實體車輛 ID、運行車次 ID、方向及軌道 ID，避免折返後將車輛與車次混為同一概念。
- [ ] 「計畫／理論結果」定義為未加入干擾、移動閉塞控制及障礙事件的基準運行；「模擬實際結果」定義為套用速限、控制與事件後的 V2 軌跡。
- [ ] 煞車距離估算預設使用營運煞車減速度，可即時切換為緊急煞車減速度；若實際侵入安全距離，保護控制仍可施加最高優先級緊急煞車。
- [ ] 播放倍率只允許降低畫面更新頻率或跳過畫面幀；V2 Engine 內部仍須依序完成每個固定 `0.1 s` 子步進，不得直接跳過控制與碰撞事件。
- [ ] 無列車空圖由運行圖檢視／匯出元件直接建立，不將 V1.0 或 V2 Simulation Engine 的有效列車數下限改為 0。
- [ ] PNG 與 CSV 優先使用 .NET／WPF 原生功能；PDF 採可隨程式離線發布、授權相容且固定版本的方案，執行時不得要求連網。
- [ ] 本軟體只能宣稱為營運與號誌概念模擬器，不得宣稱符合真實 ATP／ATO／ATS、安全完整性等級或鐵路安全認證。
- [ ] 未取得特定路線的曲率、坡度、超高、速限、列車性能及實際運行資料前，只能使用人工輸入與假設參數，不得宣稱已重現該路線真實營運。

### 建議實作順序

1. 建立 V2 多相位速度軌跡及相容介面，保住 V1.0 基準結果。
2. 加入里程區間速限與提前煞車。
3. 建立有狀態的多列車 `SimulationWorld`、事件紀錄與可重播機制。
4. 加入移動閉塞監視／控制、障礙物急停及碰撞防護。
5. 最後以統一的軌跡與事件資料建立運行圖及匯出功能。

## 1. 導入更實際的捷運速度曲線

- [ ] 保留現有三角形／梯形曲線作為「基礎物理模式」，另增「實際營運模式」。
- [ ] 實際營運模式納入加加速度（Jerk）限制、牽引力隨速度遞減、可選惰行，以及分段進站煞車。
- [ ] UI 可切換模式，並可設定最大加速度、一般減速度、Jerk、惰行比例及進站控制參數。
- [ ] 正常運行時速度、加速度與位置須連續；抵站時速度為 0，位置不得越站。障礙物急停的瞬間停止依第 0 節視為例外事件。
- [ ] 補齊短區間、長區間、無巡航、含惰行及平順停車的自動化測試。
- [ ] 若要宣稱對應特定捷運路線，須再以該路線的速限、坡度、列車性能與實際站間運行時間校準。

### 驗收條件

1. 基礎模式的既有計算與測試結果不受影響。
2. 實際營運模式不會在相位切換時產生速度或位置跳躍。
3. Simulation Engine 與解析結果的抵達時間、距離誤差保持在明確容許範圍內。
4. UI 能顯示實際營運模式的加速、惰行、煞車與平順停車階段。
5. 新增案例全部通過，並完成 Windows UI 操作驗收。

## 2. 新增以里程區間定義的路段速限

### 需求目的

不同路段可能因轉彎曲率、道岔、坡度或其他路線條件而有不同速限。速限不得只以站間區間為單位，必須能指定路線上任意「起始里程至結束里程」的限制範圍。

本項先實作人工設定的里程區間速限；曲率、超高及列車性能自動推算容許速度不在本階段範圍內。

### 資料定義

- [ ] 新增「路段速限」資料，至少包含起始里程、結束里程、速限、適用方向及備註。
- [ ] 里程以路線起點為 `0.00 km`，沿路線累加，不因車站位置重新歸零。
- [ ] 起訖里程輸入與顯示精度為 `0.01 km`（10 m）；內部計算統一換算為公尺。
- [ ] 速限區間可從任何里程開始或結束，允許跨越車站及跨越多個站間區間，車站不必是區間邊界。
- [ ] 適用方向支援「雙向」、「上行」與「下行」；預設為雙向。
- [ ] 沒有設定區間速限的位置，以列車最高速度作為上限。
- [ ] 多個速限區間重疊時，採用其中最低速限，避免因資料順序產生不安全結果。

### 模擬規則

- [ ] 列車當下容許速度為「列車最高速度」與「目前有效路段速限」中的較低值。
- [ ] Engine 必須向前檢查即將進入的較低速限區間，提早計算煞車起點，使列車在速限區間起始里程前降至規定速度，不得進入區間後才開始減速。
- [ ] 離開較低速限區間後，才可依目前速度曲線與下一個速限條件重新加速。
- [ ] 若速限起點、終點或煞車點落在站間區間內，Simulation Tick 必須正確切分運行階段，不得把整個站間區間套用同一速度。
- [ ] 實際營運模式啟用後，速限變化的加減速也必須遵守 Jerk 與分段煞車限制。
- [ ] 車站停靠規則優先；即使車站位於速限區間內，抵站速度仍必須為 0。

### UI 與輸入驗證

- [ ] UI 新增路段速限表格，可新增、編輯及刪除起始里程、結束里程、速限、方向與備註。
- [ ] 路線圖及速度曲線應標示速限區間、邊界與速限值，讓使用者能核對設定位置。
- [ ] 起始里程必須小於結束里程，兩者須位於 `0.00 km` 至路線總長度內；速限必須為有限正數。
- [ ] 不符合 `0.01 km` 精度、超出路線範圍、零長度區間或無效方向時，須阻止模擬並顯示可定位的中文錯誤訊息。
- [ ] 重疊區間不視為輸入錯誤，但 UI 應提示重疊範圍實際採用最低速限。

### 驗收條件

1. 可建立不以車站為邊界的速限，例如 `1.25 km～1.87 km，限速 45 km/h`。
2. 速限區間跨越車站或多個站間區間時，仍能依連續里程正確套用。
3. 列車在進入較低速限區間的起點時已符合速限，區間內不得超速。
4. 兩個速限重疊時，重疊部分自動採用較低值；不同方向只套用各自有效的限制。
5. 速度曲線能看出提前煞車、受限運行及離開區間後重新加速的過程。
6. 補齊區間邊界、跨站、重疊、方向別、提前煞車及錯誤輸入的自動化測試，並完成 Windows UI 操作驗收。

## 3. 加入移動閉塞、相鄰列車距離與安全包絡線

### 需求目的與範圍

在每個 Simulation Tick 即時計算同方向、同一軌道上相鄰列車的實際淨距與動態安全距離，顯示列車追蹤狀態，並在啟用控制時限制後車速度或觸發煞車。

本功能屬營運與號誌概念模擬，用於方案比較與視覺分析；未納入真實系統的完整列車完整性偵測、ATP／ATO／ATS、聯鎖、通訊失效、黏著係數認證及失效安全設計，不得宣稱為可部署的真實鐵路安全系統。

### 資料與距離定義

- [ ] 列車模型新增車長、一般煞車率、緊急煞車率、控制反應時間、定位誤差及安全餘裕等參數。
- [ ] 每列車的位置必須明確定義為車頭里程；依行駛方向與車長推算車尾里程。
- [ ] 相鄰列車依「同方向、同一軌道、沿行駛方向的位置順序」判定，分為前車（Leader）與後車（Follower）。
- [ ] 實際淨距定義為後車車頭至前車車尾的可用距離，不得以兩車中心或車頭對車頭距離取代。
- [ ] 同時保留車頭間距、實際淨距及時間間隔，UI 必須標明單位，避免混淆。
- [ ] 上下行預設視為不同軌道，分別計算相鄰關係；除非未來另行建立單線共用或交叉渡線模型，否則不同方向列車不互相形成移動閉塞配對。

### 動態安全距離

- [ ] 每個固定 `0.1 s` Simulation Tick 重新計算所有相鄰列車配對，不得只在發車或抵站時計算。
- [ ] 動態安全距離至少納入後車速度、控制反應時間、後車制動距離、前車最不利制動情境、列車定位誤差與固定安全餘裕。
- [ ] 坡度、路段速限與實際營運速度曲線完成後，安全距離計算亦須使用當下路段條件及可用煞車能力。
- [ ] 安全距離不得小於設定的絕對最小淨距；所有計算值須保留原始精度，畫面再依顯示單位格式化。
- [ ] 相鄰關係因發車、停站、折返、抵達終點或列車退出模擬而改變時，必須在同一 Tick 更新配對，不得殘留舊前車。

### 障礙物急停與煞車距離情境

- [ ] 新增「前車撞上障礙物急停」的最不利測試情境；觸發時將指定前車於當下位置立即降為 `0 km/h`，其車身占用區間固定為不可穿越的障礙範圍。
- [ ] 此情境刻意採用比一般列車緊急煞車更保守的瞬間停止假設，用於檢查後車是否具備足夠停車空間；UI 須清楚標示這不是前車的正常物理減速曲線。
- [ ] 使用者可選擇目標前車及觸發時間，也可在播放中按下「障礙物急停」按鈕立即觸發；事件發生後須保留時間、位置與列車編號。
- [ ] 後車所需停車距離至少包含控制／通訊反應距離、煞車建立期間距離、受 Jerk 限制的制動距離、定位誤差及固定安全餘裕。
- [ ] 煞車距離估算預設採用「營運煞車減速度」，並提供「緊急煞車減速度」切換按鈕；目前模式須在按鈕及圖例上清楚顯示。
- [ ] 切換煞車模式後，須以同一模擬時間及列車狀態立即重算煞車距離、預估停止里程、安全裕度與碰撞預測，不得強迫使用者重新開始模擬。
- [ ] 基礎模式可使用明確記錄假設的解析公式；實際營運模式須沿用 Jerk、坡度、黏著條件及目前選定的營運／緊急煞車能力逐步積分，不得只用固定 `v² / 2a` 取代完整曲線。
- [ ] 煞車模式按鈕控制的是估算與比較曲線；若列車實際侵入安全距離，移動閉塞控制仍可依保護規則施加最高優先級的緊急煞車。
- [ ] 每個 Tick 計算後車的預估停止里程；若預估停車端點加上安全餘裕越過前車車尾，須標記為「預測碰撞」，並計算預估侵入距離與剩餘反應時間。
- [ ] 若後車實際到達障礙範圍，模擬須停止該車、記錄碰撞事件及撞擊瞬間速度，不得讓兩列車穿越或繼續交換位置。
- [ ] 障礙物急停的保守需求應另行計算，不得覆蓋一般移動閉塞安全距離；兩者須能在同一畫面比較。

### 煞車距離圖形呈現

- [ ] 在「相鄰列車距離－時間圖」增加獨立的「障礙物情境煞車需求距離」曲線，與實際淨距、一般動態安全距離使用不同線型及圖例；曲線名稱須附註目前採用營運或緊急煞車。
- [ ] 障礙物情境煞車需求線須標示反應距離、煞車建立距離、純制動距離、安全餘裕及合計值；使用者可切換顯示分項或總和。
- [ ] 路線圖須由後車車頭向前畫出預估煞車包絡線，端點為預估停止里程；同時標示前車車尾／障礙邊界，使兩個端點能直接比較。
- [ ] 每個局部煞車線段須標示後車起煞里程、預估停止里程、障礙邊界里程、總煞車需求距離及剩餘安全裕度；里程顯示至 `0.01 km`，距離以公尺顯示。
- [ ] 當預估停止端點接近障礙邊界時以黃色呈現，越過邊界時改為紅色，並在圖上標出預測碰撞點與預估侵入距離。
- [ ] 時間圖須保留障礙事件觸發點、開始制動點、最小安全裕度點、完全停止點或碰撞點，不得因繪圖降採樣而遺失。

### 監控與控制狀態

- [ ] 保留現有「獨立運行」作為基準模式，新增「移動閉塞監視」及「移動閉塞控制」模式。
- [ ] 監視模式只計算、繪圖及告警，不改變列車運行，供使用者比較理論班距與安全需求。
- [ ] 控制模式須依安全包絡線計算後車當下允許速度；實際允許速度取列車性能、里程區間速限及移動閉塞允許速度中的最低值。
- [ ] 狀態至少分為「安全」、「接近警戒」、「需要制動」及「侵入安全距離」，並使用一致的綠、黃、橙、紅色呈現。
- [ ] 控制模式預測即將侵入安全距離時，須提前降低牽引或施加煞車，不得等實際淨距低於安全距離才反應。
- [ ] 若已侵入安全距離，應記錄事件並採最高優先級的保護制動；不得產生負淨距、穿越前車或列車順序互換而沒有碰撞事件。

### 圖形與端點值呈現

- [ ] 路線圖在每一對相鄰列車之間繪製可辨識的距離線段，以後車車頭與前車車尾作為該局部區間的兩個端點。
- [ ] 每個局部區間須標示後車車頭里程、前車車尾里程、實際淨距、動態安全距離及目前安全狀態；里程顯示至 `0.01 km`，距離以公尺顯示。
- [ ] 距離線段及標籤顏色須隨安全狀態變化；列車較密集時可避免文字重疊，但不得隱藏紅色警示。
- [ ] 新增「相鄰列車距離－時間圖」，每一列車配對各有一條實際淨距曲線，並同時顯示動態安全距離曲線。
- [ ] 時間圖須標出每段配對關係的起點、終點、目前端點、局部最小值、局部最大值及安全門檻交越點；滑鼠指向任一取樣點時可查看時間、列車編號、兩端里程與距離值。
- [ ] 相鄰配對改變時不得錯誤連接兩段不相干曲線，應結束舊線段並以新配對建立新的局部線段。
- [ ] 提供方向、列車配對、時間範圍及安全狀態篩選；大量 Tick 資料可為繪圖降採樣，但端點、局部極值及門檻交越點必須保留。

### 告警、摘要與輸入驗證

- [ ] 顯示目前最小實際淨距、最小安全裕度、最接近的列車配對，以及整段模擬的最低安全裕度與發生時間。
- [ ] 安全裕度定義為「實際淨距－動態安全距離」；小於 0 時視為侵入安全距離。
- [ ] 每次狀態升級或恢復須留下事件紀錄，包含時間、方向、前後車編號、兩端里程、速度、實際淨距、安全距離及控制動作。
- [ ] 車長、營運／緊急煞車率、反應時間、定位誤差及安全餘裕須為有限且合理的非負／正數；緊急煞車減速度不得小於營運煞車減速度，輸入錯誤時阻止模擬並顯示可定位的中文訊息。
- [ ] UI 須說明本功能為模擬估算，安全距離與警報不得解讀為真實捷運系統的認證結果。

### 驗收條件

1. 三列以上同方向列車運行時，每個 Tick 都能依位置順序形成正確的相鄰配對，且不會漏算或重複配對。
2. 路線圖能對每一局部間隔標出後車車頭、前車車尾的里程，以及實際淨距與安全距離。
3. 距離－時間圖正確保留配對線段的起訖端點、局部極值與安全門檻交越點，配對改變時不跨段誤連。
4. 後車速度提高、反應時間增加或煞車能力降低時，計算出的動態安全距離應合理增加。
5. 監視模式不改變既有運行結果；控制模式會在預測侵入安全包絡線前提前限速或煞車。
6. 停站、發車、折返、列車退出及上下行同時運行時，相鄰關係與圖表均能正確更新。
7. 觸發前車障礙物急停後，後車的反應距離、煞車距離、預估停止里程及剩餘安全裕度均能逐 Tick 更新並以獨立曲線呈現。
8. 初始估算必須使用營運煞車；切換成緊急煞車後須立即更新曲線與端點，且相同狀態下的估算停車距離不得大於營運煞車結果。
9. 空間充足時後車能在障礙邊界前停妥；空間不足時能預警、計算侵入距離，並在碰撞點停止而不穿越前車。
10. 補齊安全、警戒、制動、侵入、營運／緊急煞車切換、障礙物急停、煞車距離分項、碰撞防護、配對切換及大量列車效能測試，並完成 Windows UI 操作驗收。

## 4. 新增列車運行圖產生、檢視與匯出功能

### 需求目的與圖面定義

依模擬結果產生鐵路常用的時間－里程運行圖，用同一張圖呈現多列車在全線的發車、區間運行、停站、折返及抵達情形，供班距、交會、追蹤與異常事件分析。

- [ ] 運行圖橫軸為時間、縱軸為全線累積里程；里程以路線起點 `0.00 km` 為基準，不因車站或行駛方向重新歸零。
- [ ] 每一實體列車依車輛 ID 顯示連續軌跡；每段營運服務另標示車次 ID。上行與下行以不同顏色或線型區分，並提供清楚圖例。
- [ ] 行駛區間顯示為隨時間變化的斜線；停站與終點折返期間顯示為固定里程的水平線段。
- [ ] 軌跡資料必須直接來自 Simulation Engine，不得用與模擬結果不同的簡化速度重新繪製。
- [ ] 運行圖同時支援「計畫／理論結果」與「模擬實際結果」；前者為無干擾基準軌跡，後者為套用速限、移動閉塞控制及事件後的 V2 軌跡。兩者並列時須使用不同線型，不得混為同一條線。

### 時間、車站與軌跡資料

- [ ] 時間顯示至少支援 `HH:mm:ss`，跨越午夜時保留日期或 `+1 日` 標記，避免時間倒退。
- [ ] 縱軸標示所有車站名稱、車站編號及里程；車站位置繪製水平格線，主要時間刻度繪製垂直格線。
- [ ] 軌跡須包含發車、抵達、停站開始、停站結束、折返開始及折返結束等事件節點。
- [ ] 速限邊界、速度曲線相位、移動閉塞制動或障礙物急停造成的變化，應保留足夠取樣點，不得因線段過度簡化而失真。
- [ ] 每條軌跡至少在起點、終點及折返點標示列車／車次編號；滑鼠指向任一位置時顯示時間、里程、速度、方向、前後車站與運行狀態。
- [ ] 使用者選取列車後，可醒目顯示其完整軌跡並淡化其他列車，不影響原始資料。

### 圖面操作與分析圖層

- [ ] 支援時間與里程軸縮放、平移、回到全線範圍及指定時間區間檢視。
- [ ] 提供方向、列車／車次、車站區間、時間範圍及運行狀態篩選。
- [ ] 可選擇顯示或隱藏停站標記、折返標記、里程區間速限、移動閉塞警戒、障礙物急停、碰撞預測及實際碰撞事件。
- [ ] 可在相鄰列車軌跡間顯示同站發車間隔、同站到達間隔或指定里程斷面的通過間隔。
- [ ] 警戒、侵入安全距離、緊急制動及碰撞等重要事件須在運行圖標出時間與位置；即使關閉一般文字標籤，紅色安全事件仍不得完全隱藏。
- [ ] 播放模擬時可選擇即時更新運行圖；暫停、快轉或拖曳時間軸後，游標與列車位置必須同步。

### 圖檔、PDF 與資料匯出

- [ ] 提供「匯出運行圖」功能，至少支援 PNG 與 PDF；另可匯出 CSV 軌跡／事件資料供外部分析。
- [ ] PNG 可選一般與高解析度；PDF 預設橫向，可選 A4／A3、時間比例、里程比例及是否分頁。
- [ ] 長時間模擬分頁時，相鄰頁面須保留時間銜接與重複的車站軸標籤，不得截斷列車軌跡而無法追蹤。
- [ ] 匯出內容須包含圖名、路線名稱、模擬起訖時間、產生時間、圖例、單位及使用的主要模擬模式。
- [ ] 若啟用里程速限或移動閉塞，匯出圖面須註明相關模式；若為障礙物急停情境，須清楚標示為模擬事件。
- [ ] CSV 至少包含列車／車次編號、模擬時間、顯示時間、里程、速度、方向、狀態、前後車站及事件類型。
- [ ] 匯出檔案前提供預覽與儲存位置選擇；取消匯出不得留下不完整檔案。

### 效能、精度與錯誤處理

- [ ] 顯示里程精度為 `0.01 km`，時間至少精確至 `0.1 s`；內部資料不得先行四捨五入後再計算。
- [ ] 圖面可為效能進行視覺降採樣，但發到、停站、折返、局部轉折、安全門檻交越及異常事件節點必須保留。
- [ ] 圖上的列車位置與事件時間須和時刻表、路線動畫及 Engine 查詢結果一致，並定義可自動驗證的數值容許誤差。
- [ ] 運行圖元件在沒有軌跡資料時應產生合理空圖；Simulation Engine 仍要求至少一列車。單列車、大量列車、跨日模擬及只有部分時間資料時，都應產生合理圖面，不得當機。
- [ ] 匯出失敗時顯示可理解的中文原因，不得覆蓋既有同名檔案，除非使用者明確確認。

### 驗收條件

1. 五站、多列車雙向運行時，運行圖能正確呈現區間斜線、停站水平線、終點折返及上下行圖例。
2. 任一列車在圖上的發到時間與里程，均與時刻表及 Simulation Engine 結果在容許誤差內一致。
3. 跨午夜運行時時間軸保持連續，且能區分當日與次日時間。
4. 篩選、縮放、平移、選取列車及播放時間游標均能正確操作，不改變模擬資料。
5. 里程速限、移動閉塞警戒、障礙物急停及碰撞事件可正確疊加於對應時間與里程位置。
6. PNG、PDF 及 CSV 均可成功匯出；圖檔文字可讀、PDF 分頁連續、CSV 數值與圖面一致。
7. 大量列車或長時間模擬下仍可操作，降採樣不會遺失發到、折返、局部轉折與安全事件。
8. 補齊時間－里程轉換、跨日、上下行、停站／折返、篩選、分頁及匯出錯誤等自動化測試，並完成 Windows UI 與輸出檔案視覺驗收。
