# 軟體版本規範

本專案依工作區 `AGENTS.md` 的軟體開發版本規則管理版本，並將可公開、可攜的規則完整記錄於本文件。

## 版本格式

版本號固定使用 `V主版本.次版本.修訂版本`，例如 `V2.1.0`。

- 初始版本為 `V1.0.0`。
- 只有 Bug 修正：修訂版本加 1，例如 `V2.1.0 → V2.1.1`。
- 包含新功能或其他變更：次版本加 1，修訂版本歸零，例如 `V2.1.1 → V2.2.0`。
- 同一進版同時包含 Bug 修正與新功能時，採次版本規則，且只調整一次。
- 只有使用者明確要求「統整進版」或指定升級主版本時，才能升主版本；不得自行升級。

## 單一版本來源

目前版本只在根目錄 `Directory.Build.props` 的 `MrtVersion` 更新。建置時會同步產生：

- .NET `Version`、`AssemblyVersion`、`FileVersion` 與 `InformationalVersion`。
- 主視窗標題及畫面版本文字。
- 自動化測試標題與版本一致性驗證。

README、QA 報告、變更紀錄與 Release 目錄仍須在正式進版時同步更新；不得只修改畫面上的版本文字。

## 正式進版流程

1. 依本次已驗收的變更判定 Bug 修訂版或功能次版本。
2. 只修改一次 `Directory.Build.props` 的 `MrtVersion`。
3. 在 `CHANGELOG.md` 新增版本、日期、功能、修正及相容性說明。
4. 同步 README、QA 報告及 Release 目錄名稱。
5. 執行 Release 建置與完整自動化測試，確認執行檔版本資訊一致。
6. 只有使用者明確要求發布時，才建立 Git commit、`v主.次.修訂` Git tag 並推送 GitHub。

存檔格式的 `schemaVersion` 與軟體版本是不同概念：前者只在專案 JSON 結構不相容或需要移轉時調整，不能拿來代替軟體版本。
