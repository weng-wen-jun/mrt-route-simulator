# 路線圖常見問題與建立／讀檔防堵清單

更新日期：2026-09-13。本文保存已發現問題及修正進度；現行待辦的唯一入口仍是 `TODO.md`，驗證結果見 `QA_REPORT.md`。建立與讀檔的接軌側別檢核已實作；舊檔缺少側別資料及人工驗收限制見下文。

## 2026-09-13 實作進度

- 兩份 V3.3 範例及 baseline 的單軌折返已改為明確的 ENTRY／折返軌／EXIT 路徑，新增實體進出軌長度。baseline 移除西端直接跨股道連接，中央上行月台調回與同站一致的位置。
- 七份 PDF 站型改用明確的營運／設施路徑產生有向連接，不再枚舉所有共享節點組合。
- 站後折返另發現設施路徑本身使用錯誤渡線，已修正範例 X1／X2 端點及兩條折返路徑；這不是刪除未使用連接即可解決的問題。
- 四段折返的圖形依實際到達／出發軌道端點錨定，避免上下行投影對同一節點給出不同水平位置而產生反折；列車、軌道與月台仍共用 edge-local 映射。
- 全12個現行範例主圖／編輯器、720／1200px已通過，配置警告必須為零，不再豁免已知錯誤。以下逐檔表保留前輪盤點，現行狀態以本節及 QA_REPORT 新紀錄為準。
- 共用 `TrackPortRules` 依獨立的 `FromPortSide`／`ToPortSide` 檢查同節點兩側。`CONNECTION-001` 拒絕同側回頭及已知／未知側別混接，`CONNECTION-002` 拒絕只填一端或非法列舉。把錯誤連接加入 ServiceRoute、facility 或移除白名單，都不能繞過有側別的檢核。合法未使用連接及停車換端保留。
- 七種站型及快速建線寫入側別；分割軌道保留外端側別並使新節點兩端相反。設施精靈延續既有側別，尾軌及袋狀軌皆產生實體進出渡線。WPF 編輯器保存這些資料，讀取／儲存／套用／runtime 共用檢核；錯誤讀檔保留原專案、模擬會話、檔名與標題。
- 12份現行範例已完整補入側別。為保留 Schema 8／Schema 7 匯入相容性，兩端皆未填側別的舊軌道仍可讀取，無法據此證明其轉向方向正確；一般讀檔不會從示意座標猜測實體側別。這類舊檔的引導補值／全面強制政策及不同 DPI、連續播放人工驗收仍列待辦。

## 前輪問題清單（2026-09-12，處理狀態已由上節更新）

| 編號 | 症狀與根因 | 本輪處理／狀態 | 未來應阻擋的位置及驗收 |
|---|---|---|---|
| RL-01 | 站後上下行以垂直線相接。共用節點不代表有足够長度的實體渡線。baseline 的 `UP-M-W → DOWN-W-M` 是已知案例。 | 不再把垂直捷徑畫成合法軌道，顯示「配線待修」；模型仍保留原資料。 | 快速建線／設施精靈應產生明確喉區與有向進路；讀檔應檢查跨股道連接的結構，不依畫面像素直接拒絕。 |
| RL-02 | 袋狀軌像回頭箭頭，或入軌即轉向另一主線。模板目前以所有端點相同的 traversal 組合產生 `DirectedConnections`。 | PDF-CentralPocket 的 `PDIN → PUOUT`、`PUIN → PDOUT` 顯示待修；可用的袋軌停車換端路徑不應一併刪除。 | `StationLayoutTemplateService` 不應以笛卡兒積產生所有轉向；依道岔允許方向列舉。讀檔檢查同側進出造成的無停車反向；合法同軌換端須明確例外。 |
| RL-03 | 渡線與主線接點出現銳角、反向分岔。PDF 前／後折返存在多餘有向連接；判斷必須包含 Forward／Reverse，不能只看 edge ID。 | 顯示待修提示；不宣稱已修正運行語義。 | 先核對 ServiceRoute、facility traversal、尋徑及安全距離是否使用該連接，再刪除或改造；對合法前／後折返補正反向案例。 |
| RL-04 | 已圓滑的線被股道配置階段覆蓋成四點折線。 | 共用最終幾何改為端點水平切線及平滑過渡；主圖與編輯器一致。 | 建立與讀檔共用同一呈現模型；以有向接點及折線內部角度做 WPF 回歸，不在 Engine 限制像素曲率。 |
| RL-05 | 同站同里程，月台卻左右錯列。先前完整範例上行月台放錯軌段，另外兩端內插未使用同一座標映射。 | 完整範例先前已修正；本輪保留明確節點配置，不以另一套里程映射覆蓋手工配置。baseline 中央站仍需後續配置檢查。 | 建站時提供同站中心／明確錯列選項；讀檔檢查平台範圍與站體關係。不可用「同站必須同一 edge」限制合法多股道。 |
| RL-06 | 停妥列車與月台差5像素。月台端點留白規則平移了整塊月台。 | 先前已移除；0／120／311.5秒三站實際控制項中心驗證保留。 | 所有圖形共用 edge-local 位置映射；最小顯示寬度只能向中心兩側展開。 |
| RL-07 | 車頭停車標被當成車體中心，或折返換端造成整車平移。 | 已有 TrainCenter／TrainFront 解析及多車長 footprint 回歸。 | 建站預設中心停點，舊檔明確保留語義；讀檔／runtime驗證整車容納與反向返回路徑，不只驗證 offset 在edge內。 |
| RL-08 | 尾軌、止衝在窄視窗出界。 | 喉區示意展開後統一縮放所有軌道，月台與列車隨同映射。 | 窄版720及一般1200px，檢查所有rail端點與控制項邊界；不同DPI另驗。 |
| RL-09 | 只測中央站或靜態圖，端點／折返時仍歪斜。 | 測試涵蓋三站停靠及所有範例的線內轉角、接軌切線；另記錄待修配置。 | 須分開記錄「畫面檢查通過」與「存在配置警告」；不能把測試綠燈當成配置全部合法。 |

## 前輪逐檔盤點（2026-09-12）

所有12檔現行範例均執行主圖／編輯器720與1200px呈現及載入檢查。以下「未觸發」只指本次角度／邊界檢查，不代表真實鐵路設計認證。

| 範例 | 本輪結果／後續 |
|---|---|
| PDF-CentralPocket | 曲線已平滑；2組方向連接待修（RL-02）。 |
| PDF-DoubleIslandFourTracks | 待避線過渡改平滑；未觸發配置警告。 |
| PDF-FrontTurnback | 渡線平滑；`DC → X2`、`UC → X2`、`X1 → DC`、`X1 → UC` 的部分有向組合待修。 |
| PDF-IslandAndSideThreeTracks | 待避線過渡改平滑；未觸發配置警告。 |
| PDF-IslandTwoTracks | 直線站場；未觸發配置警告。 |
| PDF-RearTurnback | 渡線平滑；TU1／TD1與X1／X2間部分有向組合待修，須按方向核對。 |
| PDF-SideTwoTracks | 直線站場；未觸發配置警告。 |
| V3.3.0-完整功能驗證範例 | 尾軌示意喉區及接線平滑；未觸發配置警告。 |
| V3.3.0-端點站前與中間站中央避車線折返檢核 | 袋軌／尾軌示意喉區展開；未觸發配置警告。 |
| V3.4.0-雙島四股快速車越行驗證 | 待避線過渡改平滑；未觸發配置警告。檔名沿革與實際三股道差異另見samples/README.md。 |
| V4.0.0-topology-baseline | 喉區／待避線平滑及防出界；西端直接跨股道待修（RL-01），中央站配置仍列RL-05。 |
| V4.0.0-完整拓撲執行驗證範例 | 待避線過渡平滑，維持先前三站停靠對位；未觸發配置警告。 |

## 原定實作順序與驗收依據

1. 為站型模板、快速建線及設施精靈建立共同的合法有向轉向產生器，取代單純共用節點即允許轉向的規則。
2. 先對既有範例產生唯讀檢查報告，列出節點、edge、方向、使用該連接的服務與設施，再決定修檔／遷移方式。
3. 在 `InfrastructureValidator`／`StationConstructionRules` 加入結構化錯誤代碼，區分資料錯誤與示意布局警告；方向與物理判斷不得依賴WPF。
4. `TopologyProjectFormat` 讀取後完整驗證候選文件；失敗保留目前UI專案與模擬會話。不得靜默刪掉有問題的連接。
5. 編輯器套用、新建、儲存與讀檔共用相同檢核；錯誤需定位到具體欄位與物件。
6. 回歸覆蓋合法換端、多車長、尾端淨空、袋軌互斥、越行及無效資料不污染目前專案，再做一般／窄版與不同DPI的桌面驗收。

前輪僅做呈現修正；2026-09-13 已修改範例實體進路並補入進出軌長度，須以最新完整 Engine 回歸及 QA_REPORT 判斷完成範圍，不可只憑 PNG 判定。
